
# The next line updates PATH for the Google Cloud SDK.
if [ -f '/home/michael/bin/google-cloud-sdk/path.bash.inc' ]; then . '/home/michael/bin/google-cloud-sdk/path.bash.inc'; fi

# The next line enables shell command completion for gcloud.
if [ -f '/home/michael/bin/google-cloud-sdk/completion.bash.inc' ]; then . '/home/michael/bin/google-cloud-sdk/completion.bash.inc'; fi


export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion

complete -C /home/michael/bin/terraform terraform

export FLYCTL_INSTALL="/home/michael/.fly"
export PATH="$FLYCTL_INSTALL/bin:$PATH"

